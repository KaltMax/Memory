{
  "name": "memoria-workers",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "migrate:local": "wrangler d1 execute memoria-db --local --file=./migrations/0001_initial_schema.sql",
    "migrate:prod": "wrangler d1 execute memoria-db --file=./migrations/0001_initial_schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "jose": "^5.2.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "vitest": "^1.0.0",
    "wrangler": "^3.84.1",
    "miniflare": "^3.20231030.0"
  }
}
